# The choices, and the reasoning that measurement overturned

Three things here could have gone more than one way: which backend decides
`GF(p)`, which solver runs, and whether the symmetries are broken. Each was
settled by running both, and in two of the three cases the measurement
contradicted an argument that sounded right. The numbers behind them are
[`measurements.md`](../measurements.md); how the encodings work is
[`method/`](../method/README.md).

| | |
|---|---|
| [`the-gf-p-backend.md`](the-gf-p-backend.md) | which encoder decides `GF(p)`, and what the demoted one is kept for |
| [`two-defaults-that-were-wrong.md`](two-defaults-that-were-wrong.md) | symmetry breaking and the solver order, both settled against the argument |
| [`the-question-order.md`](the-question-order.md) | the fourth choice, which a paper had already answered |

Where the rank sits between the bounds, and the prices:
[`../search/README.md`](../search/README.md). What the field had settled:
[`../search-in-the-literature/README.md`](../search-in-the-literature/README.md).
