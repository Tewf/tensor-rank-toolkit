# OPTIONS/

The flag tables, one page per question, plus the four pages that come before
any table: one question per command, precedence, the recipes people actually
type, and the flags that share a spelling without sharing a meaning.

The front door and the index of all of it is
[`../OPTIONS.md`](../OPTIONS.md); this folder is its tables. Lost entirely?
[`../start-here.md`](../start-here.md) first.
